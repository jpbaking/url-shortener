# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:password@postgres:5432/shorturl

# The redirect domain, used to construct the short URL returned to the user.
# No trailing slash. Assembled by Compose from S_SCHEME and S_DOMAIN in the root .env;
# set this manually only when running the backend standalone outside Docker Compose.
REDIRECT_DOMAIN=http://s.url
